- name: auditor-action
# You may pin to the exact commit or the version.
# uses: GrantBirki/auditor-action@f8940c96520a64fdd1b693baa0fe9f5f869a0e55
uses: GrantBirki/auditor-action@v2.0.4
with:
# The path to the auditor's config file
config: # default is auditor.yml
# The path to the json diff file
json_diff_path: # default is git-diff-action-output.json
